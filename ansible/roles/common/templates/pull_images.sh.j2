#!/bin/bash

ARG=$1

if [ "$ARG" = "" ]; then
  ARG=latest
fi

docker pull {{ docker_user }}/core:$ARG
docker pull {{ docker_user }}/nginx:$ARG
docker pull rabbitmq:3.5.3-management
docker pull redis
docker pull mongo
